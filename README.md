Instructions are 16-bit, and there's not many of them. There's a simple assembler which assembles to bytecode which is then executed.

There are 8 registers, including one which is always zero. Memory is unsigned 16-bit integers, so it does not support floating point and negative values.
